apiVersion: apps/v1
kind: Job
metadata:
  name: space-launch-job
spec:
  selector:
    matchLabels:
      app: space-launch
  template:
    metadata:
      labels:
        app: space-launch
    spec:
      containers:
        - name: space-launch-container
          image: imdow/devopsrepo:latest
          command: ["dotnet"]
          args: ["SpaceLaunch.dll"]
